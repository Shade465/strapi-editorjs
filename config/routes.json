{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "wysiwyg.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/fetchUrl",
      "handler": "wysiwyg.fetchUrl",
      "config": {
        "policies": []
      }
    }
  ]
}
